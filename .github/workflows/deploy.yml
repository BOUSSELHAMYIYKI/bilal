name: Deploy

on:
  workflow_run:
    workflows: ["Test"]
    types:
      - completed
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/download-artifact@v4
        with:
          name: site
          path: dist

      - name: List deployed files
        run: ls -la dist/

      - name: Deploy
        run: echo "✅ Website deployed successfully to production!"
